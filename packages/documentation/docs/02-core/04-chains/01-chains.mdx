---
sidebar_label: chains
slug: /core/chains/chains
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

<!-- components -->
import SectionBreak from '@site/src/components/SectionBreak';

# chains

> Gets the supported chains.

<SectionBreak />

## Usage

<Tabs
  defaultValue="main"
  values={[
    { label: 'main.ts', value: 'main' },
  ]}>
  <TabItem value="main">

    ```typescript
    import { type Chain, Voi } from '@kibisis/chains';
    import { createKatavault } from '@kibisis/katavault-core';

    const katavault = await createKatavault({
      chains: [Voi],
    });
    let chains: Chain[];

    await katavault.authenticateWithPasskey({
      user: {
        name: 'Kibi',
        username: 'kibi@kibis.is',
      },
    });

    chains = katavault.chains();

    console.log(chains);
    /*
    [
      {
        displayName: 'Voi Network',
        namespace: 'avm',
        reference: 'r20fSQI8gWe_kFZziNonSPCXLwcQmH_n',
        ...
      },
    ]
    */
    ```

  </TabItem>
</Tabs>

<SectionBreak />

## Returns

`Chain[]`

The supported chains.
