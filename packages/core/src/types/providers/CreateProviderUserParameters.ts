/**
 * @property {string} name - The name of the user.
 * @property {string} username - The username of the user.
 */
interface CreateProviderUserParameters {
  name: string;
  username: string;
}

export default CreateProviderUserParameters;
